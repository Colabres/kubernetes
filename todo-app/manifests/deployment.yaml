apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: project
  name: todo-app-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: todo-app
  template:
    metadata:
      labels:
        app: todo-app
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: todo-data-claim
      containers:
        - name: todo-app
          image: todo-app
          imagePullPolicy: Never

          env:
            - name: PORT
              value: "3000"
          volumeMounts: 
            - name: data
              mountPath: /usr/src/app/files



